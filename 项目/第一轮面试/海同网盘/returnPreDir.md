
我们使用 **lastIndexOf ('/')** 函数来寻找上一级目录也就是**斜线**的位置
只要将文件路径删除到这个位置 index
通过QString.remove (index, length) 实现就行

有意思的是我们需要在返回到上一级目录之后刷新显示的文件, 所以我定义了一个
[[flushHandler]] 函数, 用来快速实现[[刷新文件夹]]的操作
其实对于
1. 刚登录进来
2. 上传文件
3. 删除文件
4. 下载文件
5. 创建文件夹
6. 删除文件夹
7. 重命名文件
8. 刷新文件夹
这些情况我认为都需要自动刷新一下文件夹, 但是老师一直都没提到

而在这里正好定义了这样一个函数
所以
- [ ] [[给其他功能加上自动刷新文件夹的操作]]