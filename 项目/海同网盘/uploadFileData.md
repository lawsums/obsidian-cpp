
对于uploadFileData这个函数来说, 其实可以当作一个**读取文件**的函数

1. 让定时器停止, 以**只读方式**打开文件, 如果文件打开失败, 那么返回warning
2. 申请一个**4096大小的缓冲区**, 如果缓冲区读到的内容**大于0而且小于等于4096就一直读下去**; 如果刚好等于零说明读取可以结束了, 如果是其他情况(读取大小大于4096或者<0)直接warning
3. 最后关闭文件file.close(), 释放缓冲区
