- [ ] cgi 会在 start. sh 之后卡死


# 1 启动
## 1.1 启动 tracker/storage
``` bash
sudo fdfs_trackerd /etc/fdfs/tracker.conf

sudo fdfs_storaged /etc/fdfs/storage.conf
```

## 1.2 启动 nginx
``` bash
sudo nginx
```

关闭
``` bash
sudo nginx -s stop
```

## 1.3 IP
192.168.174.129