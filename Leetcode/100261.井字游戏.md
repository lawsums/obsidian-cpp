---
Date: 2026-05-02
Link: https://leetcode.com/problems/tic-tac-toe-lcci/
Category: 
- 数组
- 计数
- 矩阵
Difficulty: Medium
SimilarQuestions:  
Completed: false
---

#数组 #计数 #矩阵 
```button
name <font color="#548dd4">nvim打开</font>
type link
action file:///E%3A%5CDocuments%5CObsidian_%5CCpp%5CLeetcode%5C100261.%E4%BA%95%E5%AD%97%E6%B8%B8%E6%88%8F.cpp
```
```button
name <font color="#4e937a">VSCode打开</font>
type link
action file:///code%20%22E%3A%5CDocuments%5CObsidian_%5CCpp%5CLeetcode%5C100261.%E4%BA%95%E5%AD%97%E6%B8%B8%E6%88%8F.cpp%22
```

`button-anki-open`   `button-anki-update`

DECK: 面试题-hot100

## 0.1 100261.井字游戏

设计一个算法，判断玩家是否赢了井字游戏。输入是一个 N x N 的数组棋盘，由字符" "，"X"和"O"组成，其中字符" "代表一个空位。

以下是井字游戏的规则：

- 玩家轮流将字符放入空位（" "）中。
- 第一个玩家总是放字符"O"，且第二个玩家总是放字符"X"。
- "X"和"O"只允许放置在空位中，不允许对已放有字符的位置进行填充。
- 当有N个相同（且非空）的字符填充任何行、列或对角线时，游戏结束，对应该字符的玩家获胜。
- 当所有位置非空时，也算为游戏结束。
- 如果游戏结束，玩家不允许再放置字符。

如果游戏存在获胜者，就返回该游戏的获胜者使用的字符（"X"或"O"）；如果游戏以平局结束，则返回 "Draw"；如果仍会有行动（游戏未结束），则返回 "Pending"。

**示例 1：**

**输入：** board = ["O X"," XO","X O"]
**输出：** "X"

**示例 2：**

**输入：** board = ["OOX","XXO","OXO"]
**输出：** "Draw"
**解释：** 没有玩家获胜且不存在空位

**示例 3：**

**输入：** board = ["OOX","XXO","OX "]
**输出：** "Pending"
**解释：** 没有玩家获胜且仍存在空位

**提示：**

- `1 <= board.length == board[i].length <= 100`
- 输入一定遵循井字棋规则

## 0.2 Notes
这个函数式写法非常有趣而且简洁规范

## 0.3 Solution 
```python
class Solution:
    def tictactoe(self, board: List[str]) -> str:
        #先判断有没有获胜的,获胜有三种情况，1，横，2，竖，3，斜着，左右斜,  
        n  = len(board)
        def check(c):
            return any((
                any([all([char == c for char in row]) for row in board]),
                any([all([char == c for char in col]) for col in zip(*board)]),
                all([board[i][i] == c for i in range(n)]),
                all([board[i][n-1-i] == c for i in range(n)])
            ))
        if check('X'):return 'X'
        if check('O'):return 'O'
        
        #在判断有没有可以下的
        if any([' ' in row for row in board]):
            return 'Pending'
        #最后既平局
        return 'Draw'
```


![[100261.井字游戏.cpp]]

END
<!--ID: 1777793954343-->

