---
Date: 2026-08-12
Link: https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/
Category: 
- 数组
- 动态规划
Difficulty: Medium
SimilarQuestions:  
- 买卖股票的最佳时机
- 买卖股票的最佳时机 II
Completed: false
---

#数组 #动态规划 
```button
name <font color="#548dd4">nvim打开</font>
type link
action file:///E%3A%5CDocuments%5CObsidian_%5CCpp%5CLeetcode%5C309.%E4%B9%B0%E5%8D%96%E8%82%A1%E7%A5%A8%E7%9A%84%E6%9C%80%E4%BD%B3%E6%97%B6%E6%9C%BA%E5%90%AB%E5%86%B7%E5%86%BB%E6%9C%9F.cpp
```
```button
name <font color="#4e937a">VSCode打开</font>
type link
action file:///code%20%22E%3A%5CDocuments%5CObsidian_%5CCpp%5CLeetcode%5C309.%E4%B9%B0%E5%8D%96%E8%82%A1%E7%A5%A8%E7%9A%84%E6%9C%80%E4%BD%B3%E6%97%B6%E6%9C%BA%E5%90%AB%E5%86%B7%E5%86%BB%E6%9C%9F.cpp%22
```

`button-anki-open`   `button-anki-update`

DECK: 面试题-hot100

## 0.1 309.买卖股票的最佳时机含冷冻期

给定一个整数数组`prices`，其中第  `prices[i]` 表示第 `i` 天的股票价格 。​

设计一个算法计算出最大利润。在满足以下约束条件下，你可以尽可能地完成更多的交易（多次买卖一支股票）:

- 卖出股票后，你无法在第二天买入股票 (即冷冻期为 1 天)。

**注意：**你不能同时参与多笔交易（你必须在再次购买前出售掉之前的股票）。

**示例 1:**

**输入:** prices = [1,2,3,0,2]
**输出:** 3 
**解释:** 对应的交易状态为: [买入, 卖出, 冷冻期, 买入, 卖出]

**示例 2:**

**输入:** prices = [1]
**输出:** 0

**提示：**

- `1 <= prices.length <= 5000`
- `0 <= prices[i] <= 1000`
## 0.2 Notes

本题有四个关键状态转移：

**持有状态（hold = True）下的两种转移：**

1. **持有 → 持有**（保持持有）：直接继承前一天的状态。
   - `dfs(i-1, True)` — 什么都不做，继续持有。

2. **未持有 → 持有**（买入）：因为有冷冻期，卖出后的第二天不能买入，所以买入时必须从 `i-2` 天且处于"未持有"状态转移过来。
   - `dfs(i-2, False) - prices[i]` — 跳过一天（冷冻期），在第 i 天买入。

**未持有状态（hold = False）下的两种转移：**

3. **未持有 → 未持有**（保持未持有）：直接继承前一天的状态。
   - `dfs(i-1, False)` — 什么都不做，继续观望。

4. **持有 → 未持有**（卖出）：卖出持有的股票，获得当天价格。
   - `dfs(i-1, True) + prices[i]` — 在第 i 天卖出。

## 0.3 Solution 
```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        n = len(prices)
        @cache
        def dfs(i: int, hold: bool) -> int:
            if i < 0:
                return -inf if hold else 0
            if hold:
                return max(dfs(i - 1, True), dfs(i - 2, False) - prices[i])
            return max(dfs(i - 1, False), dfs(i - 1, True) + prices[i])
        return dfs(n - 1, False)
```


![[309.买卖股票的最佳时机含冷冻期.cpp]]

END
