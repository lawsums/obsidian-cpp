

   虚函数是在基类中声明的、允许子类重写的函数（用 [[virtual]] 关键字修饰）。  
   实现方式：  
   - 编译器为每个包含虚函数的类生成一个**[[虚函数表]]（vtable）**，存储该类所有虚函数的地址；  
   - 类的每个对象会包含一个**[[虚指针]]（vptr）**，指向所属类的虚函数表；  
   - 当通过基类指针/引用调用虚函数时，==程序通过vptr找到对应的vtable，再调用实际类型的函数实现==，实现运行时多态。