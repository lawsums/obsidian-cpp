一句话说清：
**悬垂引用（Dangling Reference）** 就是：**引用指向的对象已经被销毁/释放了，但引用本身还在使用**。

简单类比：
你有一张**门牌号**（引用），但房子（对象）早就被拆了/回收了，你还按这个地址找人，结果就是**非法访问、程序崩溃**。

常见场景：
- 函数返回局部变量的引用
- 对象被 delete 后，还在用它的引用
- 容器扩容/删除元素后，旧迭代器/引用失效还继续用

特点：
- 编译不一定报错
- 运行时**未定义行为**（崩溃、乱值、看似正常）

要不要我用一段**极简 C++ 代码**给你演示悬垂引用长什么样？