# 1 打卡
```easy-tracker-daily-overview
```
```easy-tracker-year-calendar-heatmap
```
```easy-tracker-buttons
  打卡 | 1
```
---
# 2 目录 


最佳实践
	使用 vscode 自己配置一个 anaconda 终端配合使用

[torchvision中的数据集使用_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1hE411t7RN?vd_source=cf6228c0b4a5c283905e22fd11934994&spm_id_from=333.788.videopod.episodes&p=14)
[04 数据操作 + 数据预处理【动手学深度学习v2】_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1CV411Y7i4?spm_id_from=333.788.player.switch&trackid=web_related_0.router-related-2589621-z4htq.1784273319279.116&vd_source=cf6228c0b4a5c283905e22fd11934994)
[线性回归的从零开始实现_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1PX4y1g7KC?spm_id_from=333.788.videopod.episodes&vd_source=cf6228c0b4a5c283905e22fd11934994&p=3)


- [x] 100_配置版本.ipynb
- [x] 101_Pytorch安装.ipynb
- [x] 102_Python两大法宝.ipynb
- [x] 103_Pytorch加载数据.ipynb
- [x] 104_Tensorboard使用.ipynb
- [x] 105_Transforms使用.ipynb
- [x] 106_torchvision数据集使用.ipynb
- [x] 107_Dataloader使用.ipynb
- [x] 108_nn.Module模块使用.ipynb
- [x] 109_卷积原理.ipynb
- [x] 110_卷积层.ipynb ✅ 2026-07-05
- [x] 111_最大池化层.ipynb ✅ 2026-07-05
- [x] 112_非线性激活.ipynb ✅ 2026-07-06
- [ ] 113_线性层及其他层.ipynb
- [x] 114_搭建小实战和Sequential使用.ipynb ✅ 2026-07-08
- [x] 115_损失函数与反向传播.ipynb ✅ 2026-07-15
- [x] 116_优化器.ipynb ✅ 2026-07-15
- [ ] 117_网络模型使用及修改.ipynb
- [ ] 118_网络模型保存与读取.ipynb


- [ ] 00	预告
- [x] 01	课程安排 ✅ 2026-07-17
- [x] 02	深度学习介绍 ✅ 2026-07-17
- [x] 03	安装 ✅ 2026-07-17
- [x] 04	数据操作 + 数据预处理 ✅ 2026-07-17
- [x] 05	线性代数 ✅ 2026-07-19
- [x] 06	矩阵计算 ✅ 2026-07-19
- [x] 07	自动求导 ✅ 2026-07-19
- [x] 08	线性回归 + 基础优化算法 ✅ 2026-07-19
- [x] 09	Softmax 回归 + 损失函数 + 图片分类数据集 ✅ 2026-07-21
- [x] 10	多层感知机 + 代码实现 ✅ 2026-07-24
	- [x] 理论 ✅ 2026-07-22
	- [x] 操作 ✅ 2026-07-24
- [ ] 11	模型选择 + 过拟合和欠拟合
- [ ] 12	权重衰退
- [ ] 13	丢弃法
- [ ] 14	数值稳定性 + 模型初始化和激活函数
- [x] 15	实战：Kaggle 房价预测 + 课程竞赛：加州2020年房价预测 ✅ 2026-07-29
- [ ] 16	PyTorch 神经网络基础
- [x] 17	使用和购买 GPU ✅ 2026-07-29
- [ ] 18	预测房价竞赛总结
- [x] 19	卷积层 ✅ 2026-07-30
- [ ] 20	卷积层里的填充和步幅
- [ ] 21	卷积层里的多输入多输出通道
- [x] 22	池化层 ✅ 2026-07-31
- [x] 23	经典卷积神经网络 LeNet ✅ 2026-08-02
- [ ] 24	深度卷积神经网络 AlexNet
- [ ] 25	使用块的网络 VGG
- [ ] 26	网络中的网络 NiN
- [ ] 27	含并行连结的网络 GoogLeNet / Inception V3
- [ ] 28	批量归一化
- [ ] 29	残差网络 ResNet
- [ ] 30	ResNet 为什么能训练出 1000 层的模型 + 第二部分完结竞赛：图片分类
- [ ] 31	深度学习硬件：CPU 和 GPU
- [ ] 32	深度学习硬件：TPU 和其他
- [ ] 33	单机多卡并行
- [ ] 34	多 GPU 训练实现
- [ ] 35	分布式训练
- [ ] 36	数据增广
- [ ] 37	微调
- [ ] 38	第二次竞赛 树叶分类 结果
- [ ] 39	实战 Kaggle 比赛：图像分类（CIFAR-10）
- [ ] 40	实战 Kaggle 比赛：狗的品种识别（ImageNet Dogs）
- [ ] 41	物体检测和数据集
- [ ] 42	锚框
- [ ] 43	树叶分类竞赛技术总结
- [ ] 44	物体检测算法：R-CNN，SSD，YOLO
- [ ] 45	SSD 实现
- [ ] 46	语义分割和数据集
- [ ] 47	转置卷积
- [ ] 47.2	转置卷积是一种卷积
- [ ] 48	全连接卷积神经网络 FCN
- [ ] 49	样式迁移
- [ ] 50	课程竞赛：牛仔行头检测
- [ ] 51	序列模型
- [ ] 52	文本预处理
- [ ] 53	语言模型
- [ ] 54	循环神经网络 RNN
- [ ] 55	循环神经网络 RNN 的实现
- [ ] 56	门控循环单元（GRU）
- [ ] 57	长短期记忆网络（LSTM）
- [ ] 58	深层循环神经网络
- [ ] 59	双向循环神经网络
- [ ] 60	机器翻译数据集
- [ ] 61	编码器-解码器架构
- [ ] 62	序列到序列学习（seq2seq）
- [ ] 63	束搜索
- [ ] 64	注意力机制
- [ ] 65	注意力分数
- [ ] 66	使用注意力机制的 seq2seq
- [ ] 67	自注意力
- [ ] 68	Transformer
- [ ] 69	BERT 预训练
- [ ] 70	BERT 微调
- [ ] 71	目标检测竞赛总结
- [ ] 72	优化算法
- [ ] 73	【完结】课程总结和进阶学习

---

# 3 笔记

## 3.1 100_配置版本

Pytorch 当前不支持 3.13 版本的 python，所以我们选择 3.12 版本下载

[如何设置在vscode终端打开conda环境_vscode配置conda环境-CSDN博客](https://blog.csdn.net/qq_64606903/article/details/138170807)
通过这个视频给 vscode 加入一个 conda 终端

---
## 3.2 101_Pytorch 安装

通过 nvidia-smi 查看当前显卡的 torch 版本

---
## 3.3 104_Tensorboard 使用
这一节需要安装 tensorboard
``` python
pip install tensorboard
```

使用以下指令进行图片查看
``` shell
tensorboard --logdir=logs 
```

---
## 3.4 105_Transforms 使用
这一节需要安装 opencv
``` python
pip install opencv-python
```

### 3.4.1 常见的 transforms
主要关注每个 transform 的输入输出

---

## 3.5 理解损失和梯度

你可以把整个网络想象成一条**流水线**：

```
1输入 x → [层1] → [层2] → [层3] → 输出 ŷ → 损失 L
```

反向传播就是：从损失 LL 开始，**逆着流水线**把"这个参数对最终损失的贡献有多大"这个信息，一层一层往回传。每一层只需要知道自己局部的导数，再乘上后面传回来的梯度，就能算出对自己的参数的梯度。

所以你说的完全正确——**简单单元拼起来 + 链式法则 = 整个网络的梯度都能算出来**。这也是为什么反向传播能在深度学习中如此成功的关键：它把"对几百万个参数求导"这个看似恐怖的事情，变成了每一层做一次简单的局部计算。

[不至于吧，梯度下降简单得有点离谱了啊！_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV19f421Q7CL?spm_id_from=333.788.recommend_more_video.0&trackid=web_related_0.router-related-2589621-49mtn.1784216081318.377&vd_source=cf6228c0b4a5c283905e22fd11934994)

## 3.6 数据操作+数据预处理
0-d 标量
1-d 向量
2-d 矩阵
3-d RGB 图片
4-d 一个批次的 RGB 图片
5-d 一个批次的视频（加上时间维度）

### 3.6.1 创建矩阵



### 3.6.2 访问元素

### 3.6.3 


## 3.7 线性回归 + 基础优化算法

### 3.7.1 线性回归模型

线性回归是所有监督学习模型中最基础的一个——用一条一次函数的直线来拟合数据。

**例子：房价预测**

选取影响房价的几个因素作为输入 $X$：
- $X_1$：卧室个数
- $X_2$：卫生间个数
- $X_3$：居住面积

将它们合成为向量 $\mathbf{X} = [X_1, X_2, X_3]$。

每个因素对房价的影响权重不同，给每个 $X$ 分配不同权重：
- $X_1$ 的权重为 $w_1$
- $X_2$ 的权重为 $w_2$
- $X_3$ 的权重为 $w_3$

权重同样合成向量 $\mathbf{W} = [w_1, w_2, w_3]$。

**模型公式：**

$$
\hat{y} = \mathbf{W} \cdot \mathbf{X} + b
$$

其中 $\hat{y}$ 是预测的理论成交价，$b$ 是偏置（bias）。理论值和实际值 $y$ 之间总会有误差。

### 3.7.2 损失函数

这个误差称为**损失（loss）**，通过损失函数来量化。一个好的损失函数能准确体现理论值与实际值的差距。

提到"差距"，最直观的就是**距离**。所以一开始我们用的损失函数是：

$$
L(\mathbf{W}, b) = \frac{1}{2}\|\hat{y} - y\|^2
$$

即理论值与实际值差的平方（二范数），非常符合直觉——差距越大，损失越大。**目标就是最小化损失**，从而找到最优的 $\mathbf{W}$ 和 $b$。

### 3.7.3 解析解

损失函数 $L$ 是一个凸函数（可类比二次函数），必定有最低点，在最低点处**导数为零**。

因此，对损失求导，令导数为零，可以直接解出 $\mathbf{W}$，这就是**解析解**——一步到位求出最优参数。

### 3.7.4 梯度下降

现实中很多模型无法直接求解析解，这时就用**梯度下降（Gradient Descent）** 迭代逼近。

**直观类比：盲人下山**

> 一个盲人在下山。他看不见，于是用脚尖 360° 转一圈，感受哪里路面最陡。最陡的方向就是**梯度最大**的方向——往那边走一步。不断重复"转一圈 → 走一步"，最终逼近山底（损失最低点）。

### 3.7.5 学习率

**学习率（learning rate）** 决定每一步迈多大。

| 学习率 | 表现 | 问题 |
|--------|------|------|
| **太小** | 每次只挪几厘米，然后重新找方向 | 花费大量时间，计算代价高 |
| **太大** | 像跳过去而非走过去 | 可能从一个山头跳到另一个山头，造成**震荡**，在两个山头间来回跳，永远到不了最低点 |

> 学习率太小太慢，太大震荡，需选取合适的值。

### 3.7.6 为什么梯度下降合理？

目标是让模型走到损失函数的最低点（山底），损失 = 当前位置到最低点的垂直距离。

每一步只能做**局部抉择**时，选择最陡峭方向下降最快。每一步都这样走，整体下降最快——不迷路、不绕弯，最高效地逼近最优解。（这当然也与学习率的选择有关。）

### 3.7.7 批次大小

**批次大小（batch size）** 也是一个重要的超参数，决定了每次用多少样本计算梯度。

## 3.8 Softmax 回归 + 损失函数
我们使用交叉熵损失计算 Softmax 回归，因为 Softmax 实际上是一个 one-hot 编码的分类问题，而交叉熵擅长的就是判断输出量和实际量的关系（？）
![[交叉熵求梯度]]

### 3.8.1 常用的损失函数这里介绍三种 
1. 平方损失
2. 绝对值损失
3. 二者结合的损失：当 |y - y'| > 1 时是绝对值损失，当 |y - y'| ≤ 1 时是平方损失

### 3.8.2 Fashion-MNIST 数据集
- [x] 下载数据集 ✅ 2026-07-22
- [ ] 可视化数据集
- [ ] 读取数据集

### 3.8.3 实验
- [ ] 预处理展平图片
- [ ] 合理定义这个权重矩阵以及偏置向量
- [ ] 实现 Softmax
- [ ] 实现交叉熵损失函数
- [ ] 实现精度评估
- [ ] 实现 updater（可选）

## 3.9 多层感知机

### 3.9.1 感知机理论
感知机其实是一个很简单的二分类模型。将权重与 x 的点积加上偏置输入到感知机中，然后感知机进行判断：如果输入值大于 0，则输出 1；否则输出 -1。
所以这个感知机是很适合二元分类问题，比如猫狗问题的。
感知机的 loss 函数策略是比较神奇的。感知机的 loss 函数是这个，相当于是在 0 和负的 y 乘 w 乘 x 的点积之间取一个最大值。
如果 y×w×x+b 的点击结果小于等于 0，则需要进行梯度更新：
1. w+ = yi×xi
2. b+ = yi
这个可以通过 loss 函数求导得出
### 3.9.2 XOR 问题
![[Pasted image 20260722220637.png]]

#### 3.9.2.1 什么是 XOR (异或)？

- **逻辑规则**：XOR 是一种逻辑运算，规则是“如果两个输入相同，结果为假（0）；如果两个输入不同，结果为真（1）”。
    - 0 和 0 → 0 (相同)
    - 1 和 1 → 0 (相同)
    - 0 和 1 → 1 (不同)
    - 1 和 0 → 1 (不同)
- **图示对应**：图片中的坐标系展示了这四个点。
    - **绿色点 (+1)**：代表结果为真的情况（即输入不同）。
    - **红色点 (-1)**：代表结果为假的情况（即输入相同）。

#### 3.9.2.2 为什么这是一个“问题”？

- **线性不可分**：请看图中的蓝色直线。无论你如何画一条直线，都无法将这两个绿色点和两个红色点完美地分开。
    - 如果你画一条线把左上的绿点分开，右下的绿点就会漏掉。
    - 这就是所谓的**“线性不可分”**。
- **感知机的局限**：图片文字提到“感知机只能产生线性分割面”。这意味着单层感知机就像一把直尺，只能画直线来分类数据。面对 XOR 这种需要“曲线”或者“折线”才能分开的数据，它就彻底失效了。

### 3.9.3 多层感知机理论
多层感知机的原理是：一层感知机无法区分的样本，可以通过多层感知机结合来解决。例如异或问题，一层感知机无法处理，因此使用两层感知机。

1. 第一层感知机相当于横轴的那条线，即 y > 0 和 y < 0 的分界。
2. 第二层感知机相当于竖着的那条线，即 x > 0 和 x < 0 的分界。
所以我们判定：
3. x > 0 且 y > 0，或者 x < 0 且 y < 0，这两个情况值为 1
4. 其余两个情况值为 0
就实现了异或。
![[Pasted image 20260723200707.png]]
这里可以看到，x 也就是输入的大小是 n，隐藏层的大小是 m，所以隐藏层和输入之间的权重矩阵大小是 m×n。输出的大小是 k，所以输出和隐藏层之间的权重矩阵大小是 m×k。


## 3.10 实战：Kaggle房价预测 + 课程竞赛：加州2020年房价预测
- [ ] rmse
- [ ] Adam

## 3.11 使用和购买 GPU
数据在同一个 GPU 上运算很快，如果转移到其他 GPU 或者 CPU 上就会很慢，为了省事，python 会直接报错

## 3.12 卷积层
我们的识别器需要有两个原则，就是需要有一个平移不变性和一个局部性。平移不变性指的是不管这个对象在图片里的哪个位置，我们都应该都能找到它。
第二个是局部性原则，即不需要看全局特征，而是针对识别器，以较小的特征就能找到对象。

![[Pasted image 20260730171230.png]]
这里为什么二维的权重矩阵变成了四维的权重矩阵？原因是原本的全连接层，输入层和输出层都是一维向量；而现在输入和输出都变成了图片。我们知道图片是二维的，所以二维加二维就是四维的权重矩阵。
后续我们又引入了一个新的索引，将绝对坐标的 K, L 重新映射为 I+A 和 j +B。
也就是把绝对索引换成了相对索引，后面的坐标就全部换成了相对索引。相对索引的概念更加清晰，它是以当前单元格为中心，通过四周单元格加上偏移量来看，这样比较容易做卷积。而之前是以零零坐标点为中心来看，那样就不适合做卷积。


![[Pasted image 20260730174634.png]]
关于平移不变性，从卷积核的角度来看，卷积本身不依赖于其他因素，卷积核是保持不变的。其中，i、j 是大矩阵的偏移量，而 a、b 是卷积核内的偏移量。因此，卷积核只依赖于 a、b 这两个因素，可以将其收缩为一个二维矩阵，无需包含 i、j。


![[Pasted image 20260730175406.png]]
这个局部性原理就相对简单了，说白了就是说卷积核的大小是有限制的。我们的 VAB 如果 A 大于 VA 或者 B 大于这个卷积核的边界的话，结果值都是 0。

## 3.13 池化层
最简单的池化层就是最大值池化层，它对池化范围内的所有单元格求最大值。
主要作用是缓解卷积层对于位置的敏感性

池化层有什么作用？池化层和卷积层想要实现的目的有什么区别？
### 3.13.1 池化层的作用
池化层的核心作用是对特征图进行**下采样（降维）**，具体包括：

- **降低计算量和参数量**：通过缩小特征图的空间尺寸（宽和高），减少后续层的计算负担。
- **增大感受野**：让后续卷积层能"看到"更大范围的输入区域。
- **提供一定的平移不变性**：正如你提到的，池化层能缓解卷积层对位置的敏感性。例如最大值池化（Max Pooling）只保留局部区域中最强的激活值，即使特征在空间上发生了小幅偏移，池化后仍能被检测到。
- **防止过拟合**：通过丢弃部分信息，起到一定的正则化效果。
### 3.13.2 池化层 vs 卷积层的目的区别

| 卷积层          | 池化层                             |                          |
| ------------ | ------------------------------- | ------------------------ |
| **核心目的**     | **特征提取**                        | **特征压缩/聚合**              |
| **做什么**      | 通过可学习的卷积核扫描输入，检测局部模式（边缘、纹理、形状等） | 对已有特征图做下采样，保留最重要的信息      |
| **是否有可学习参数** | ✅ 有（卷积核权重和偏置）                   | ❌ 通常没有（操作是固定的，如取最大值或平均值） |
| **输出通道数**    | 可以改变（由卷积核数量决定）                  | 不改变（通道数保持不变）             |
| **空间尺寸**     | 可以通过步幅和填充控制，但主要目的不是缩小           | 主要目的就是缩小空间尺寸             |


> **卷积层负责"发现特征"，池化层负责"提炼和压缩特征"。**

## 3.14 深度卷积神经网络 AlexNet
- AlexNet赢了2012年ImageNet 竞赛
- 更深更大的 LeNet
- 主要改进：
	- 数据增强
	- sigmoid->丢弃法ReLu
	- AvgPooling->MaxPooling
	- 计算机视觉方法论的改变
### 3.14.1 🖼️ 左侧路径：传统机器学习方法

这是深度学习爆发之前的主流做法，流程比较繁琐，依赖人工智慧。

1. **原始图片输入**：比如这是一张猫的照片。
2. **人工特征提取**：这是最关键的一步。程序员需要手动告诉计算机什么是“猫”。比如：寻找边缘、检测角点、描述纹理（毛茸茸的）、形状（尖耳朵）等。这需要深厚的数学和图像处理知识（如SIFT, HOG算法）。
3. **SVM (支持向量机)**：提取出来的特征数据（比如一长串数字）会被送给 SVM 这个分类器。SVM 的任务很简单：根据你给的特征，判断它是“猫”还是“狗”。

- **缺点**：如果人设计的特征不够好（比如没考虑到猫在不同光线下的颜色变化），识别率就低。而且不同任务需要设计不同的特征，工作量巨大。

### 3.14.2 🚀 右侧路径：深度学习方法

这是现在的主流（也就是大家常说的 AI / 深度学习）。

1. **原始图片输入**：同样是一张猫的照片。
2. **通过 CNN 学习特征**：
    - **CNN (卷积神经网络)** 代替了人类工程师。
    - 你不需要告诉机器什么是猫，而是直接把图片和答案（这是猫）喂给 CNN。
    - 网络会自动通过层层计算，自己摸索出什么样的特征代表猫（底层学边缘，高层学眼睛、鼻子）。这叫**端到端的学习**。
3. **Softmax 回归**：CNN 提取出高级特征后，最后通常接一个 Softmax 层。它的作用是把特征转化为概率。比如输出：90%是猫，5%是狗，5%是兔子。
---



* 2026-02-21 - 1
* 2026-07-03 - 1
* 2026-07-05 - 1
* 2026-07-06 - 1
* 2026-07-08 - 1
* 2026-07-16 - 1
* 2026-07-17 - 1
* 2026-07-18 - 1
* 2026-07-19 - 1
* 2026-07-21 - 1
* 2026-07-22 - 1
* 2026-07-23 - 1
* 2026-07-24 - 1
* 2026-07-25 - 1
* 2026-07-28 - 1
* 2026-07-29 - 1
* 2026-07-30 - 1
* 2026-07-31 - 1
* 2026-08-02 - 1