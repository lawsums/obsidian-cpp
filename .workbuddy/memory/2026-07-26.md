# 2026-07-26 工作日志

## Bangumi 番剧导入 CLI 脚本（Obsidianvault）
- 用户(lawsam)要求把 `Templates/ACGbangumiv2.1.js`（Obsidian QuickAdd 版，依赖 Notice/request/DOMParser/quickAddApi）改写成纯命令行 js 脚本，方便接入 Obsidian 的大模型调用并生成 Project 笔记。要求：不改动其它文件。
- 关键路径：Obsidian 主 vault 在 `E:/Documents/Obsidian/`（`Cpp` 工作区里的 `Templates` 是指向它的符号链接）。番剧笔记落在 `Projects/{anime,manga,game}/`，文件名=中文名。QuickAdd 用的模板是 `Templates/T-{动画,漫画,游戏}.md`。
- 产出新文件 `E:/Documents/Obsidian/Templates/ACGbangumi-cli.cjs`：
  - 忠实移植原脚本 searchBangumi / getAnimeByurl / getComicByurl / getGameByurl / extractBaseInfo / parseCharacterList / extractInfoboxFields，字段与逻辑逐一对应。
  - 去依赖 Obsidian/QuickAdd：Notice→stderr、request→零依赖 httpGet、DOMParser→linkedom、交互 prompt→CLI 参数、executeChoice→本地读模板渲染并写文件。
  - 子命令 `search`（输出 JSON 供大模型选择）与 `add`（`--url` 直接抓或 `--name` 自动取首条）。`--dry-run` 预览、`--json` 结构化输出、`--vault/--folder/--filename/--out/--template` 可覆盖路径。
  - 模板渲染：替换 `{{VALUE:field}}`、`{{DATE:fmt}}`，剥离 Templater `<%* ... -%>` 代码块并替换 `<% var %>` 交互变量（state/mediaInfo/subGroup/subLanguage/catego，均有默认值可被 CLI 覆盖）。
- 依赖与网络坑：
  - linkedom 装在 workbuddy 托管 node 工作区 `C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules/linkedom`（沙箱拦了 npm cache，需 dangerouslyDisableSandbox 安装）。脚本多路径回退加载。
  - 本机有代理 `HTTP_PROXY=http://127.0.0.1:7890`，但 Node 原生 fetch 不读代理 env 会超时。改用零依赖 httpGet（http/https/tls/zlib + CONNECT 隧道），自动读 HTTP_PROXY/HTTPS_PROXY/NO_PROXY，并支持 `--proxy` 与 `BGM_PROXY`。
  - 必须 `Connection: close`（否则 keep-alive 不关连接导致读响应体挂起）；requestGet 加 45s 超时兜底。
- 校验：对 `bgm.tv/subject/431767`(GIRLS BAND CRY) dry-run 输出与现有 `Projects/anime/GIRLS BAND CRY.md` 逐字段比对——结构/章节列表/角色表/简介完全一致；仅 CN名、首别名、封面 http→https 因页面数据随时间漂移而不同（属忠实抓取当前页面）。漫画(葬送的芙莉莲)、游戏(艾尔登法环)路径也验证通过。
- bug 修复：`--dry-run` 因 kebab-case 未转 camelCase 而失效（曾误写一个测试笔记，已删）；parseArgs 已加 toCamel 归一化。
