# 2026-07-27 工作日志

## ACGbangumi-cli.cjs 修复

- **linkedom 懒加载**：原来是模块顶层 `const { parseHTML } = loadLinkedom()` ——如果 linkedom 找不到，连 `--help` 都直接 exit(2) 没机会输出。改成 `getParseHTML()` 懒加载，只在 `parseHtmlToDom` 首次调用时才加载。`--help` / `search`（不解析 HTML 时）完全不受 linkedom 影响。
- 另一个 Agent 跑 `--help` 没输出 — 是它自己的 PowerShell stdout 捕获问题（`echo "hello world"` 也出不来），不是脚本 bug。本 WorkBuddy 环境验证 `--help` 正常，exit 0，输出完整。

## Agent 调用协议

用户反馈 Obsidian 内嵌 Agent 调用脚本很麻烦（路径混乱、输出捕获不到）。创建了两个辅助文件：

- **`ACGbangumi.cmd`** — Windows 批处理包装器。Agent 用 PowerShell 调 `cmd.exe /c ACGbangumi.cmd --help` 或直接在调用时指定 `.cmd`，比裸 `node script.cjs` 更稳定（CMD 子进程 stdout 不受 PowerShell 宿主捕获问题影响）。自动 cd 到脚本目录、优先用 workbuddy 托管 node。
- **`ACGbangumi-API.md`** — 完整的调用协议文档。Agent 可加载为上下文，含所有子命令、参数表、示例流程、输出路径约定。用户只需让 Agent 读这个文件就知道怎么调。

## 按协议捕获《间谍过家家》

- 使用 ACGbangumi-cli.cjs 标准流程：`search --name "间谍过家家" --type anime --json` → 得 14 条结果，选第一季 `subject/329906`
- `add --url https://bgm.tv/subject/329906 --type anime` → 成功写入 `E:/Documents/Obsidian/Projects/anime/间谍过家家.md`
- 标签：搞笑、日常、WITSTUDIO、CloverWorks、家庭喜剧 等 30+ 标签
- 角色列表完整（劳埃德/约尔/阿尼亚 + 6 位配角，含 CV 和头像）
- 章节列表完整（12 话，含中/日标题）
- 搜索到的其他条目可供后续捕获：第2部分(373267)、第二季(411427)、第三季(498378)、剧场版(411428)

## 批量添加 2026年7月新番（7部，用户筛选）

全部以"想看⏰"状态、search→add 标准流程写入：

| # | 番名 | Bangumi | 文件 |
|---|------|---------|------|
| 1 | 恋爱游戏世界对路人角色很不友好 第二季 | subject/412144 | 7/8 播出 |
| 2 | 从后面来的神威先生 | subject/627136 | 7/3 播出 |
| 3 | Re:从零开始的异世界生活 第四季 夺还篇 | subject/633836 | 8/12 播出 |
| 4 | 超超超超超喜欢你的100个女朋友 第三季 | subject/598058 | 7/5 播出 |
| 5 | 骸骨骑士大人异世界冒险中 第二季 | subject/528828 | 7/6 播出 |
| 6 | 攻壳机动队 THE GHOST IN THE SHELL | subject/496276 | 7/7 播出 |
| 7 | 穹庐下的魔女 | subject/552533 | 7/4 播出 |

路径: `E:\Documents\Obsidian\Projects\anime\*.md`
