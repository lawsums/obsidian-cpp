DECK: 嵌入式 interview

{Q}C 语言程序的基本单位是什么？{A}函数{E}
<!--ID: 1787842385904-->


{Q}在 32 位和 64 位系统中，int 类型各占多少字节？{A}32 位占 4 字节，64 位占 4 字节（都是 4 字节）{E}
<!--ID: 1787842385911-->


{Q}Linux 中 umask 设置为 022 时，新创建的普通文件权限是什么？{A}-rwxr-xr-x（实际是 755，但文件默认无执行权限，所以是 -rw-r--r--，注意需结合默认权限 666 计算）{E}
<!--ID: 1787842385919-->


{Q}使用哪个命令可以检测基本的网络连接？{A}ping{E}
<!--ID: 1787842385927-->


{Q}以下哪个命令不是用来显示文件内容的？A.more B.file C.less D.cat{A}B（file 用于查看文件类型）{E}
<!--ID: 1787842385933-->


{Q}STM32 通用定时器的时基单元包含哪些寄存器？{A}TIMx_ARR（自动重装载寄存器）、TIMx_RCR（重复次数寄存器）、TIMx_PSC（预分频器寄存器）、TIMx_CNT（计数器寄存器）{E}
<!--ID: 1787842385941-->


{Q}C 语言中，const int var; var = 5; 这样声明常量是否正确？为什么？{A}不正确。const 变量必须在声明时初始化，之后不能赋值。{E}
<!--ID: 1787842385947-->


{Q}父进程被撤销时，其所有子进程是否也被相应撤销？{A}不是。子进程会被 init（PID=1）收养，成为孤儿进程，继续运行。{E}
<!--ID: 1787842385954-->


{Q}一个子进程可以同时从属于多个父进程吗？{A}不可以。每个子进程只有一个父进程（PPID 唯一）。{E}
<!--ID: 1787842385960-->


{Q}C 语言函数可以返回数组本身吗？可以返回指向数组的指针吗？{A}不可以返回数组本身，但可以返回指向数组的指针（如 int*）。{E}
<!--ID: 1787842385967-->


{Q}C 语言函数可以返回 union（联合体）类型吗？{A}可以。C 函数不能返回的只有数组和函数本身。{E}
<!--ID: 1787842385975-->


{Q}定义 `int a[5] = {1,2,3,4,5}; int *ptr = (int*)(&a + 1); printf("%d", *(ptr - 1));` 输出结果是什么？{A}5（&a+1 跳过整个数组，指向末尾之后，ptr-1 回退一个 int 指向 a[4]）{E}
<!--ID: 1787842385982-->


{Q}数组名 a 和 &a 的步长有什么区别？{A}a 是 int* 类型，a+1 跳 4 字节（1 个元素）；&a 是 `int(*)[5]` 类型，&a+1 跳整个数组大小（20 字节，假设 int 占 4 字节）。{E}
<!--ID: 1787842385990-->


{Q}定义 `int a[8]`; 以下哪个不可以表示 `a[1]` 的地址？A.(int*)(&a+1) B.(int*)((char*)&a+sizeof(int)) C.&a[0]+1 D.a+sizeof(int){A}A 和 D。A 跳过整个数组指向末尾之后；D 跳 4 个元素指向 `a[4]`。{E}
<!--ID: 1787842385997-->


{Q}在 64 位 Linux 中，long 占多少字节？在 64 位 Windows 中呢？{A}64 位 Linux（LP64）：8 字节；64 位 Windows（LLP64）：4 字节。建议用 int64_t 保证跨平台。{E}
<!--ID: 1787842386005-->


{Q}C 语言中，while(1) {} 是什么？{A}死循环{E}
<!--ID: 1787842386013-->


{Q}`int a[][3]` = {{1,2,3}, {4,5}, {6,7}}; 数组 a 的第一维大小是多少？{A}3（因为每行 3 列，共 3 行）{E}
<!--ID: 1787842386021-->


