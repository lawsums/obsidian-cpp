# 1 深信服嵌入式软件开发笔试 —— 本地自测用例

本目录对应 [[笔试—深信服嵌入式软件开发笔试]] 中的 3 道编程题，提供 **输入/输出测试文件** 与 **一键检测脚本**，写完代码后自行验证。

## 1.1 目录结构

```
深信服笔试-测试/
├── README.md            # 本文件
├── run_tests.bat        # 一键检测入口（Windows 双击 / 命令行）
├── run_tests.ps1        # 测试脚本本体（自动发现题目与用例）
├── q1.cpp               # 空白答题模板（已建好，直接往里写）
├── q2.cpp               #   有几道题就有几份
├── q3.cpp
├── reference/           # 参考实现（用于自检测试环境是否正常）
│   ├── q1.cpp
│   ├── q2.cpp
│   └── q3.cpp
├── q1-数组游戏连胜数字/    # 题目1 用例：test1.in / test1.out ...
├── q2-最大排序连续上升子段/ # 题目2 用例
└── q3-病毒伪装度/          # 题目3 用例
```

> 每题的 `test1` 都是笔记里的样例，其余为补充的边界用例。`.in` 是输入，`.out` 是期望输出。
> `qN.cpp` 已自动创建为**可直接编译的脚手架**（含输入读取），打开即可写算法。

## 1.2 题目与输入格式

| 题号 | 题目 | 输入格式（stdin） | 输出格式（stdout） |
| --- | --- | --- | --- |
| q1 | 数组游戏——找出连胜 k 场的数字 | 一行，n 个数字用空格隔开；前 n-1 个组成 `arr`，最后一个是 `k` | 一个整数：连胜 k 场的数字 |
| q2 | 最大排序连续上升子段长度 | 第一行 `n`，第二行 `n` 个整数 | 一个整数：最大合法子段长度 |
| q3 | 病毒伪装度最少变换次数 | 第一行 `n x`，第二行 `n` 个整数 `a[i]` | 一个整数：最少变换伪装度的次数 |

## 1.3 使用方法

前置条件：已安装 **MinGW-w64**，且 `g++` 已加入 PATH。

1. **写代码**：直接在已建好的 `q1.cpp`、`q2.cpp`、`q3.cpp` 里填写（脚手架的 `// TODO: 在这里实现` 处）。文件已按题目数量建好，无需自己新建。
2. **跑测试**（在 cmd 或 PowerShell 中，双击 `run_tests.bat` 亦可）：

   ```bat
   run_tests.bat                :: 自动测试全部题目（每题自动找 qN.cpp）
   run_tests.bat -Problem q1    :: 只测题目1
   run_tests.bat -List          :: 只列出发现了哪些题目
   ```

   也可以指定任意源文件（跑所有题目）：

   ```bat
   run_tests.bat -Source my_solution.cpp
   ```

3. 每条用例输出 `PASS` / `FAIL` / `TODO` / `TIMEOUT`，最后给出汇总；失败时会打印期望输出与实际输出方便对照。
4. **写一道测一道**：还没实现的题目显示 `TODO`（程序无输出），**不算失败**；只有写错的才 `FAIL`。

### 1.3.1 用参考实现自检

第一次使用建议先验证测试环境本身（`reference\qN.cpp` 是笔记里的标准答案）：

```bat
run_tests.bat -Reference
```

全部应为 `PASS`，说明编译器与用例都正常。

## 1.4 用例清单

| 题目 | 用例 | 考察点 |
| --- | --- | --- |
| q1 | test1 ~ test4 | 笔记中的 4 组样例（含 k 远大于 n 的剪枝分支） |
| q1 | test5 ~ test6 | 最小规模（arr 长度 2）+ k=1 立即结束 |
| q1 | test7 | 严格递增数组，k = n-1（不触发剪枝） |
| q1 | test8 | 递减数组，最大值一开始就在队首 |
| q1 | test9 ~ test10 | k=1 快速命中；含 0 的元素 |
| q2 | test1 | 笔记样例 |
| q2 | test2 ~ test3 | n=1；全相同元素（答案 1） |
| q2 | test4 | 整段严格递增（答案 = 整段长度） |
| q2 | test5 | 整段不连续，只能取前段（答案 4） |
| q2 | test6 | 重复元素触发剪枝 |
| q2 | test7 | 取值上界 10⁹ 附近的边界 |
| q2 | test8 ~ test10 | 答案在末尾；n=2 相邻 / 相同 |
| q3 | test1 | 笔记样例（答案 0） |
| q3 | test2 | n=1（初始伪装不计次数） |
| q3 | test3 | 需要变化 2 次 |
| q3 | test4 | `a[i] + x` 溢出 32 位（需 `long long`） |
| q3 | test5 | 需要变化 1 次 |
| q3 | test6 ~ test7 | 全相同元素；x=0 |
| q3 | test8 | 等差数列，变化 2 次 |
| q3 | test9 ~ test10 | 区间收缩后失效；x=0 且全相同 |

## 1.5 说明

- **比对规则**：忽略空白差异（多余空格、Tab、换行都不影响），但**数字序列必须完全一致**。
- **添加用例**：在对应题目目录里加 `testN.in` + `testN.out` 即可，脚本自动扫描 `test*.in`（按数字排序）。
- **超时保护**：单条用例默认 10 秒超时，防止死循环卡住。
- 每题样例（`test1`）与笔记中的示例输入/输出一致，可直接对照。
