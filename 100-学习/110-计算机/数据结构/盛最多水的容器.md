
DECK: 面试题-hot100

## 盛最多水的容器

![[Pasted image 20251122171952.png]]

给定一个长度为 `n` 的整数数组 `height` 。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])` 。

找出其中的两条线，使得它们与 `x` 轴共同构成的容器可以容纳最多的水。

返回容器可以储存的最大水量。

**说明：**你不能倾斜容器。


## 思路

这题在两个柱子确定的时候，决定能盛多少水取决于**较矮的一根柱子**
也就是**短板效应**，所以移动较高的柱子是没有好处的，而**如果移动较小的柱子得到更高的柱子则能更新答案**

## 标准解法

```python
from common_imports import *

class Solution:
    def maxArea(self, height: List[int]) -> int:
        n = len(height)
        l = 0
        r = n - 1
        ans = 0

        while l < r:
            # print(f"l, r = {l}, {r}")
            # print(f"\theight[l] = {height[l]}, height[r] = {height[r]}")
            ans = max(ans, 
                      min(height[l], height[r]) * (r - l)
                      )
            if height[l] < height[r]:
                l += 1
            else:
                r -= 1
            # print(f"\tcur = {min(height[l], height[r]) * (l - r)}")
            # print(f"\tans = {ans}")

        return ans
        
```

## 我的解法

我一开始使用错了，前后缀的方法这道题行不通


END
<!--ID: 1763803809892-->

