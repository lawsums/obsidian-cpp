

DECK: 面试题-hot100

## 字符串相加

给定两个字符串形式的非负整数 `num1` 和`num2` ，计算它们的和并同样以字符串形式返回。

你不能使用任何內建的用于处理大整数的库（比如 `BigInteger`）， 也不能直接将输入的字符串转换为整数形式。

**示例 1：**

**输入：** num1 = "11", num2 = "123"
**输出：**"134"

**示例 2：**

**输入：** num1 = "456", num2 = "77"
**输出：**"533"

**示例 3：**

**输入：** num1 = "0", num2 = "0"
**输出：**"0"

**提示：**

- `1 <= num1.length, num2.length <= 104`
- `num1` 和`num2` 都只包含数字 `0-9`
- `num1` 和`num2` 都不包含任何前导零
---

相关标签

[数学](https://leetcode.cn/tag/math/) [字符串](https://leetcode.cn/tag/string/)[模拟](https://leetcode.cn/tag/simulation/)

#字符串
#数学
#模拟

---

相似题目

 [两数相加](https://leetcode.cn/problems/add-two-numbers/)

中等

 [字符串相乘](https://leetcode.cn/problems/multiply-strings/)

中等

 [数组形式的整数加法](https://leetcode.cn/problems/add-to-array-form-of-integer/)
 
## 思路

这道题不难但是需要注意一下
1. 对于字符串来说都是**大端存储**，所以我们需要先**翻转**字符串
2. 后续计算就像**链表相加**一样
3. 要注意**数字**和**字符串**之间的**转换**
4. 要记得 **i++**，所以最好使用 for 而不是 while

## 标准解法

```cpp
class Solution {
public:
    string addStrings(string num1, string num2) {
        reverse(num1.begin(), num1.end());
        reverse(num2.begin(), num2.end());
        
        int n1 = num1.size(), n2 = num2.size();
        string ans;
        ans.reserve(max(n1, n2) + 1);  // 预分配空间，避免多次重新分配
        
        int carry = 0;
        for (int i = 0; i < max(n1, n2) || carry; i++) {
            int cur = carry;
            if (i < n1) cur += num1[i] - '0';
            if (i < n2) cur += num2[i] - '0';
            carry = cur / 10;
            ans.push_back('0' + (cur % 10));
        }
        
        reverse(ans.begin(), ans.end());
        return ans;
    }
};

```

## 我的解法


END
<!--ID: 1764899441082-->

