
# 1 Introduction
低级视觉任务很重要，它包括以下功能：
图像超分辨率 [1]、去模糊 [2]、去雾 [3]、图像修复 [4]、图像融合 [5]、压缩感知 [6]、低光照增强 [7]，以及遥感图像中的去云 [8]。

- [ ] 看看这个引用文献是不是每个方面的综述


> **"Traditional approaches [14], [15] framed low-level vision problems as variational optimization challenges and utilized handcrafted algorithms to enforce proximity constraints related to specific image properties or degradation priors [16]–[19]."**

**中文翻译：**  
“传统方法 [14], [15] 将低级视觉问题构建为变分优化挑战，并利用手工设计的算法来施加与特定图像属性或退化先验相关的邻近约束 [16]–[19]。”

这句话的意思很简单，就是说传统方法是利用手工设计算法，那肯定接下来就要铺垫我们引入这个机器学习和深度学习的方法。


**你提供的原文：**

> "However, these methods often struggle to handle complex degradations due to their limited generalizability. With the rise of deep learning, convolutional neural networks..."

**中文翻译：**  
“然而，由于**泛化(generalizabitlity)** 能力有限，这些方法往往难以处理复杂的**退化**（问题）。随着深度学习的兴起，卷积神经网络……”



深度**卷积神经网络(CCN)**，以及 **Transformer** 出色的特征提取能力，被广泛用于各种低级视觉任务中。他们尽管这些方法得到了一些不错的成绩，但他们在**纹理生成**方面表现不佳。
为了解决这个限制，深度生成模型，尤其是**生成对抗网络(GAN)**，被引入到低级视觉处理任务中。它能够生成逼真的纹理细节，从而适用于现实视觉场景。不过，它也面临一些关键挑战。
一、训练过程容易陷入模式崩溃状态，优化过程也不稳定，需要复杂的超参数调优。
二、生成的结果往往包括不符合实际情况的细节，从而影响了它的应用价值。

近年来，扩散模型[25]–[33]因其出色的生成能力和训练稳定性，已成为计算机视觉领域中一种极具前景的替代方案。扩散模型通过正向扩散过程向数据中添加噪声，再通过反向扩散过程去除这些噪声，从而生成高质量的样本。与 GAN 不同，扩散模型属于基于概率的模型，其训练目标可以被视为一种加权后的变分下界。这一设计带来了诸多优势：**更广泛的分布覆盖能力、更稳定的训练目标，以及更易于扩展**的特性。

卷积神经网络、Transformer->生成对抗网络->扩散模型

- 第 2 节首先全面介绍了各种扩散模型，明确了它们与其他深度生成模型之间的联系。
- 第 3 节则总结了在自然低级视觉任务中应用的各类基于扩散模型的先进方法，根据其底层框架和目标任务对它们进行了分类，涵盖了六种常见的应用场景。
- 第 4 节将研究范围扩展到医学成像、遥感以及视频处理领域，全面介绍了扩散模型在这些领域的应用情况。
- 第 5 节回顾了低级视觉任务中常用的评估标准和指标，并对六种代表性任务中基于扩散模型的技术的性能进行了全面、定量的评估。
- 最后，第 6 节中，我们指出了目前基于 DM 的方法所存在的主要局限性，并提出了未来研究的四个方向。
- 第 7 节则对全文进行了总结。

# 2 扩散模型的详解

Denoising diffusion probabilistic models (DDPMs),



# 3 实验

# 4 未来研究方向DMs的局限性

# 5 Conclusion
