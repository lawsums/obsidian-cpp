## 0.1 环形多线程安全队列
变量
1. mutex_
2. tail_
3. head_
4. capacity_
5. not_empty_
6. not_full_

条件变量
notify_one
``` cpp
not_empty_.wait(lock, [this] {
	return !is_empty();
});
```
通过完美转发匹配左右值
`std::forward<U>(item)`
``` cpp
template <typename U>
void push (U &&item) {
	// 上一个锁, 控制并发
	std::unique_lock<std::mutex> lock(mutex_);
	
	// 等待信号量
	not_full().wait(lock, [this] {
		return !is_full();
	});
	
	buffer_[tail_] = std::forward<U>(item);
	tail_ = (tail_ + 1) % capacity_;
	not_empty_.notify_one();
}
```

## 0.2 手撕 unique

变量
1. ptr

移动赋值
``` cpp
unique_ptr &opreator=(unique_ptr &&that) {
	if (this == &that) {
		return *this;
	}
	
	delete ptr_;
	ptr_ = std::exchange(that.ptr_, nullptr);
	return *this;
}
```

移动构造
``` cpp
unique_ptr(unique_ptr &&that) {
	ptr_ = std::exchange(that.ptr_, nullptr);
}
```

拷贝赋值禁用!
拷贝构造禁用!

重载->/\*/get ()
重载 bool
release
reset
swap

数组版本特化
全局 swap
全局比较函数

## 0.3 完美转发

## 0.4 红黑树和 AVL 树

根叶黑
不红红
黑路同
左根右

## 0.5 虚拟内存

内存分段并通过某种映射方式转化为物理内存
段标识符是由一个16位长的字段组成，称为段选择符。其中前13位是一个索引号。后面3位包含一些硬件细节, 所以有 8192 段而不是 65536 段
通过全局 GDT 找到该段的段描述符, base 字段是该字段的起始地址
GDT: 全局段号记录表
LDT:  局部段号记录表

# 1 段标识符（段选择符） vs 段描述符：核心区别
这两个是x86段式内存管理中**完全不同层级、功能完全独立**的两个核心结构，我用「快递地址」类比帮你彻底分清：

---

## 1.1 一、核心定义与本质
### 1.1.1 段标识符（Segment Identifier = 段选择符 Segment Selector）
- **本质**：**16位的「索引凭证」**，是CPU指令中直接使用的逻辑地址的一部分
- **作用**：用来**定位段描述符**，相当于快递地址里的「门牌号+楼栋号」，只负责找到目标段的描述信息
- **结构**（16位）：

| 位范围   | 字段          | 作用                          |
| ----- | ----------- | --------------------------- |
| 15~3位 | 索引号（Index）  | 在GDT/LDT中定位段描述符的下标          |
| 2位    | TI（表指示器）    | 0=GDT（全局描述符表），1=LDT（局部描述符表） |
| 1~0位  | RPL（请求者特权级） | 访问权限校验，决定当前请求的特权等级          |
- **存储位置**：存在CPU的段寄存器（CS/DS/SS/ES/FS/GS）中，是程序运行时直接操作的对象

---

### 1.1.2 段描述符（Segment Descriptor）
- **本质**：**8字节（64位）的「段属性档案」**，存储在GDT/LDT中，是段的完整元数据
- **作用**：**描述一个段的完整属性**，相当于快递地址里的「完整收件人信息+权限+边界」，包含段的起始地址、大小、权限等
- **核心字段**：

| 字段          | 作用                           |
| ----------- | ---------------------------- |
| Base（基地址）   | 段在内存中的**线性起始地址**（32位，分三部分存储） |
| Limit（段限长）  | 段的最大偏移量（20位，决定段的大小）          |
| Type（类型）    | 段的类型（代码段/数据段/系统段）、读写权限       |
| DPL（描述符特权级） | 段本身的特权等级（0~3，0最高）            |
| G（粒度位）      | Limit的单位（0=字节，1=4KB页）        |
| P（存在位）      | 段是否在物理内存中                    |
  
- **存储位置**：仅存在于内存的GDT（全局描述符表）或LDT（局部描述符表）中，CPU不会直接操作它，只会通过段选择符索引读取

---

## 1.2 二、核心区别对照表
| 对比维度 | 段标识符（段选择符） | 段描述符 |
|----------|----------------------|----------|
| **大小** | 16位（2字节） | 64位（8字节） |
| **核心作用** | 索引、定位段描述符 | 描述段的完整属性（基地址、大小、权限等） |
| **存储位置** | CPU段寄存器（CS/DS等） | 内存中的GDT/LDT表 |
| **生命周期** | 随程序运行动态切换（如任务切换时更新段寄存器） | 系统初始化时创建，长期存在（GDT全局，LDT随任务） |
| **类比** | 快递的「取件码」 | 快递的「完整包裹信息（收件人、地址、重量、权限）」 |
| **CPU操作** | 程序直接加载到段寄存器 | CPU仅通过选择符索引读取，不直接修改 |

---

## 1.3 三、完整地址转换流程（帮你串联两者关系）
1. 程序给出**逻辑地址**：`[段选择符 : 段内偏移量]`
2. CPU用**段选择符**的「索引号+TI位」，从GDT/LDT中找到对应的**段描述符**
3. 从段描述符中取出**Base（段基地址）**，和段内偏移量相加，得到**线性地址**
4. （开启分页时）再将线性地址转换为物理地址

---

## 1.4 四、关键补充（避坑点）
1. **段选择符≠段描述符**：段选择符只是「钥匙」，段描述符才是「锁对应的门信息」
2. **权限校验逻辑**：CPU会用段选择符的`RPL` + 段描述符的`DPL` + CPL（当前特权级）做三重校验，确保访问合法
3. **段描述符的Base字段**：就是你图中提到的「段的起始地址」，是地址转换的核心

---

要不要我再给你补一个**x86段式地址转换的完整步骤拆解**，或者用一个实际的数值例子（比如具体的段选择符值、段描述符值）帮你算一遍线性地址？

---

# 2 请你说一说进程和线程的区别
1. 进程是资源分配的最小单位, 线程是~~执行任务~~ cpu 调度的最小单位
2. 进程有独立的系统资源, 线程共享大部分系统资源, 包括堆\代码段\数据段, 每个线程只拥有一些在运行中必不可少的私有属性, 比如 tcb, 线程 ID, 栈, 寄存器等.
3. 进程死亡, 其他进程不会跟着死亡; 线程死亡, 其他同一进程里的线程会跟着死亡
4. 进程开销大, 线程开销小
5. 进程之间通信复杂, 线程之间通信简单

# 3 请问单核机器上写多线程程序，是否需要考虑加锁，为什么？
需要, 因为单核上面执行任务是分配时间片执行的, 可能相邻的任务之间会抢占资源, 所以需要加锁防止资源被抢占.

# 4 死锁发生的条件
条件:
1. <font color="#d83931">互斥条件</font>
2. <font color="#d83931">不可剥夺条件</font>
3. 循环等待
4. <font color="#d83931">请求和保持条件</font>

解决方法:
1. 一次性申请所有资源
2. 按顺序申请资源
3. 主动放弃资源

# 5 换页策略
FIFO
LRU
LFU
LRU-k
**ARC**

# 6 锁有哪些
lock
condition varible
read-write lock
spinlock
scoped_lock
lock_guard
unique_mutex

要理清这些锁/同步原语的分类，核心是先建立**分类维度**（按「设计目的」「使用方式」「底层实现」「所有权语义」），再把每个概念归到对应类别里——这样既好记，又能理解它们的设计初衷和适用场景。

### 6.1.1 一、核心分类维度 + 具体归属
先给出总览表，再逐个维度拆解：

| 分类维度                | 包含的概念                                                                 |
|-------------------------|----------------------------------------------------------------------------|
| 1. 底层同步原语（核心能力） | lock（泛称）、spinlock（自旋锁）、read-write lock（读写锁）、condition variable（条件变量） |
| 2. C++ RAII 封装器（使用方式） | lock_guard、scoped_lock、unique_lock（注意：你写的 unique_mutex 是笔误，正确是 unique_lock） |
| 3. 按「阻塞方式」        | 阻塞锁：普通互斥锁（lock）、读写锁、条件变量依赖的互斥锁<br>自旋锁：spinlock |
| 4. 按「访问粒度」        | 排他锁：普通互斥锁、spinlock、unique_lock/lock_guard 封装的互斥锁<br>共享-排他锁：read-write lock |

### 6.1.2 二、逐类别详细解释
#### 6.1.2.1 维度1：底层同步原语（提供核心同步能力，是“基础砖”）
这是操作系统/标准库提供的**核心同步工具**，直接控制线程间的竞争与协作：
1. **lock（互斥锁，通常指 std::mutex）**
   - 最基础的**排他锁**（也叫互斥量）：任意时刻只能有一个线程持有锁，其他线程尝试加锁会阻塞，直到锁被释放。
   - 核心作用：保护临界区，防止多线程同时修改共享数据。
2. **spinlock（自旋锁）**
   - 特殊的排他锁：线程尝试加锁失败时，**不阻塞休眠**，而是在用户态“循环自旋”（不断检查锁是否释放），直到拿到锁。
   - 适用场景：临界区执行时间极短（< 微秒级），避免线程上下文切换的开销；反之，长临界区用自旋锁会浪费CPU。
3. **read-write lock（读写锁，C++17 后 std::shared_mutex）**
   - 「共享-排他」锁（也叫共享互斥锁），区分“读操作”和“写操作”：
     - 读锁（共享锁）：多个线程可同时持有，适合只读场景；
     - 写锁（排他锁）：只能一个线程持有，写时禁止所有读/写。
   - 核心优势：读多写少的场景下，比普通互斥锁效率更高（避免读操作互相阻塞）。
4. **condition variable（条件变量，std::condition_variable）**
   - 不是“锁”，而是**线程间协作工具**：配合互斥锁使用，让线程“等待某个条件满足”（比如“队列非空”），条件不满足时释放锁并阻塞，直到被其他线程唤醒。
   - 核心作用：解决“轮询等待”问题（比如线程不用一直循环检查队列是否有数据），减少CPU浪费。

#### 6.1.2.2 维度2：C++ RAII 封装器（管理锁的生命周期，是“安全套”）
这些是 C++ 标准库对「底层锁（如 std::mutex）」的**封装类**，核心目的是通过 RAII（资源获取即初始化）自动管理锁的加锁/解锁，避免手动 unlock 导致的死锁（比如异常跳出临界区时忘记解锁）。
> 注意：你写的「unique_mutex」是笔误，正确是 `std::unique_lock`（没有 unique_mutex 这个类型）。

1. **lock_guard**
   - 最简单的 RAII 封装器：
     - 构造时自动加锁，析构时自动解锁；
     - 不可手动解锁、不可转移所有权（生命周期和作用域绑定，出作用域就解锁）。
   - 适用场景：简单临界区，只需“作用域内加锁，出作用域解锁”。
2. **scoped_lock（C++17 引入）**
   - 增强版 lock_guard：支持**同时加锁多个互斥锁**，且能避免死锁（内部用“锁排序”算法）。
   - 比如：`scoped_lock lock(mtx1, mtx2);` 会自动按安全顺序加锁，避免线程A先锁mtx1再锁mtx2，线程B先锁mtx2再锁mtx1导致的死锁。
   - 替代：C++17 前需手动用 `std::lock(mtx1, mtx2)` + lock_guard，scoped_lock 更简洁。
3. **unique_lock**
   - 最灵活的 RAII 封装器：
     - 支持「延迟加锁」（构造时不加锁，后续手动调用 `lock()`）；
     - 支持手动解锁（`unlock()`）、重新加锁；
     - 可配合条件变量使用（condition_variable 的 `wait()` 方法要求传入 unique_lock）；
     - 所有权可转移（支持移动语义，比如 `std::move(lock)`）。
   - 适用场景：需要灵活控制锁的生命周期（比如条件变量等待、中途解锁），代价是比 lock_guard 稍占内存。

#### 6.1.2.3 维度3：按「阻塞方式」分类
- **阻塞锁**：线程加锁失败时，会放弃CPU，进入内核态休眠，直到锁被唤醒（比如 std::mutex、读写锁）。
  - 优点：不浪费CPU；缺点：上下文切换有开销（几十微秒级）。
- **自旋锁**：加锁失败时，线程不休眠，循环检查锁状态（用户态自旋）。
  - 优点：无上下文切换开销；缺点：自旋时占用CPU，适合短临界区。

#### 6.1.2.4 维度4：按「访问粒度」分类
- **排他锁（独占锁）**：任意时刻只能有一个线程持有，所有操作（读/写）都互斥（比如 std::mutex、spinlock）。
- **共享-排他锁**：区分读/写，读操作共享，写操作独占（比如读写锁 std::shared_mutex）。

### 6.1.3 三、易混概念澄清
1. **“lock”的歧义**：
   - 泛称：所有锁的统称（比如“加锁”动作）；
   - 特指：std::mutex（基础互斥锁）。
2. **unique_mutex 是笔误**：
   - C++ 里只有 `std::unique_lock`（RAII 封装器）和 `std::mutex`（底层互斥锁），没有 `unique_mutex`。
3. **条件变量不是锁**：
   - 条件变量必须配合互斥锁使用（比如 wait() 时会先解锁，被唤醒后重新加锁），它的核心是“等待/唤醒”，而非“保护临界区”。

### 6.1.4 总结
1. **底层原语是核心**：mutex（lock）、spinlock、read-write lock 提供同步能力，condition variable 提供线程协作能力；
2. **RAII 封装器是安全工具**：lock_guard（简单）、scoped_lock（多锁）、unique_lock（灵活）封装底层锁，避免手动解锁的坑；
3. **选型关键**：短临界区用 spinlock，读多写少用读写锁，简单临界区用 lock_guard，灵活控锁用 unique_lock，多锁加锁用 scoped_lock。
---

单开一个进程, 因为线程死了会影响其他线程, 不能给用户这样的体验
为了保证不同用户之间不会相互影响, 所以应该开辟一个进程

---
1. <font color="#d83931">互斥条件</font>
2. 循环等待
3. 不可剥夺
4. 不可放弃

---
1. 按顺序拿取资源
2. 一次拿取所有资源
3. 可以放弃资源

---
# 7 get 和 post 有什么区别
1. get 主要是获取资源, post 主要是提交信息


