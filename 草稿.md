## 0.1 环形多线程安全队列
变量
1. mutex_
2. tail_
3. head_
4. capacity_
5. not_empty_
6. not_full_

条件变量
notify_one
``` cpp
not_empty_.wait(lock, [this] {
	return !is_empty();
});
```
通过完美转发匹配左右值
`std::forward<U>(item)`
``` cpp
template <typename U>
void push (U &&item) {
	// 上一个锁, 控制并发
	std::unique_lock<std::mutex> lock(mutex_);
	
	// 等待信号量
	not_full().wait(lock, [this] {
		return !is_full();
	});
	
	buffer_[tail_] = std::forward<U>(item);
	tail_ = (tail_ + 1) % capacity_;
	not_empty_.notify_one();
}
```

## 0.2 手撕 unique

变量
1. ptr

移动赋值
``` cpp
unique_ptr &opreator=(unique_ptr &&that) {
	if (this == &that) {
		return *this;
	}
	
	delete ptr_;
	ptr_ = std::exchange(that.ptr_, nullptr);
	return *this;
}
```

移动构造
``` cpp
unique_ptr(unique_ptr &&that) {
	ptr_ = std::exchange(that.ptr_, nullptr);
}
```

拷贝赋值禁用!
拷贝构造禁用!

重载->/\*/get ()
重载 bool
release
reset
swap

数组版本特化
全局 swap
全局比较函数

## 0.3 完美转发

## 0.4 红黑树和 AVL 树

根叶黑
不红红
黑路同
左根右

## 0.5 虚拟内存

内存分段并通过某种映射方式转化为物理内存
段标识符是由一个16位长的字段组成，称为段选择符。其中前13位是一个索引号。后面3位包含一些硬件细节, 所以有 8192 段而不是 65536 段
通过全局 GDT 找到该段的段描述符, base 字段是该字段的起始地址
GDT: 全局段号记录表
LDT:  局部段号记录表

# 1 段标识符（段选择符） vs 段描述符：核心区别
这两个是x86段式内存管理中**完全不同层级、功能完全独立**的两个核心结构，我用「快递地址」类比帮你彻底分清：

---

## 1.1 一、核心定义与本质
### 1.1.1 段标识符（Segment Identifier = 段选择符 Segment Selector）
- **本质**：**16位的「索引凭证」**，是CPU指令中直接使用的逻辑地址的一部分
- **作用**：用来**定位段描述符**，相当于快递地址里的「门牌号+楼栋号」，只负责找到目标段的描述信息
- **结构**（16位）：

| 位范围   | 字段          | 作用                          |
| ----- | ----------- | --------------------------- |
| 15~3位 | 索引号（Index）  | 在GDT/LDT中定位段描述符的下标          |
| 2位    | TI（表指示器）    | 0=GDT（全局描述符表），1=LDT（局部描述符表） |
| 1~0位  | RPL（请求者特权级） | 访问权限校验，决定当前请求的特权等级          |
- **存储位置**：存在CPU的段寄存器（CS/DS/SS/ES/FS/GS）中，是程序运行时直接操作的对象

---

### 1.1.2 段描述符（Segment Descriptor）
- **本质**：**8字节（64位）的「段属性档案」**，存储在GDT/LDT中，是段的完整元数据
- **作用**：**描述一个段的完整属性**，相当于快递地址里的「完整收件人信息+权限+边界」，包含段的起始地址、大小、权限等
- **核心字段**：

| 字段          | 作用                           |
| ----------- | ---------------------------- |
| Base（基地址）   | 段在内存中的**线性起始地址**（32位，分三部分存储） |
| Limit（段限长）  | 段的最大偏移量（20位，决定段的大小）          |
| Type（类型）    | 段的类型（代码段/数据段/系统段）、读写权限       |
| DPL（描述符特权级） | 段本身的特权等级（0~3，0最高）            |
| G（粒度位）      | Limit的单位（0=字节，1=4KB页）        |
| P（存在位）      | 段是否在物理内存中                    |
  
- **存储位置**：仅存在于内存的GDT（全局描述符表）或LDT（局部描述符表）中，CPU不会直接操作它，只会通过段选择符索引读取

---

## 1.2 二、核心区别对照表
| 对比维度 | 段标识符（段选择符） | 段描述符 |
|----------|----------------------|----------|
| **大小** | 16位（2字节） | 64位（8字节） |
| **核心作用** | 索引、定位段描述符 | 描述段的完整属性（基地址、大小、权限等） |
| **存储位置** | CPU段寄存器（CS/DS等） | 内存中的GDT/LDT表 |
| **生命周期** | 随程序运行动态切换（如任务切换时更新段寄存器） | 系统初始化时创建，长期存在（GDT全局，LDT随任务） |
| **类比** | 快递的「取件码」 | 快递的「完整包裹信息（收件人、地址、重量、权限）」 |
| **CPU操作** | 程序直接加载到段寄存器 | CPU仅通过选择符索引读取，不直接修改 |

---

## 1.3 三、完整地址转换流程（帮你串联两者关系）
1. 程序给出**逻辑地址**：`[段选择符 : 段内偏移量]`
2. CPU用**段选择符**的「索引号+TI位」，从GDT/LDT中找到对应的**段描述符**
3. 从段描述符中取出**Base（段基地址）**，和段内偏移量相加，得到**线性地址**
4. （开启分页时）再将线性地址转换为物理地址

---

## 1.4 四、关键补充（避坑点）
1. **段选择符≠段描述符**：段选择符只是「钥匙」，段描述符才是「锁对应的门信息」
2. **权限校验逻辑**：CPU会用段选择符的`RPL` + 段描述符的`DPL` + CPL（当前特权级）做三重校验，确保访问合法
3. **段描述符的Base字段**：就是你图中提到的「段的起始地址」，是地址转换的核心

---

要不要我再给你补一个**x86段式地址转换的完整步骤拆解**，或者用一个实际的数值例子（比如具体的段选择符值、段描述符值）帮你算一遍线性地址？

---
