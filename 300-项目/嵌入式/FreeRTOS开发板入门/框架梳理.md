
```easy-tracker-daily-overview
```
```easy-tracker-year-calendar-heatmap
```
```easy-tracker-buttons
  打卡 | 1
```

---
# 1 目录 

---
# 2 笔记
## 2.1 数据传输的方法_环形buffer
用这个环境 buffer 来表示，用于传输数据。一开始写指针和读指针都是 0，此时表示空。如果写满了，理论上来说 w 不是要绕一圈吗？这个时候 w 又和 r 相等了，但是这个时候表示满就和空冲突了。所以我们会有一个位置故意不写，也就是在写指针的下一个指针是读指针的时候，我们表示满。
**环形缓冲区的本质作用是：写操作只访问写指针，读操作只访问读指针，不会在同一时间对同一个资源进行争夺，所以环形缓冲区可以相对安全地传输数据。**


### 2.1.1 数据传输的方法_队列的本质
![[Pasted image 20260811223717.png]]

### 2.1.2 队列实验_多设备玩游戏
对于多个设备之间的通信，我们可以将不同设备的消息存储到一个大队列中。

以红外旋转编码器为例，其中断消息的处理流程如下：
1. 在中断触发时，先将消息发送到一个小队列中。
2. 再通过这个小队列，将旋转编码器的消息转发到大队列中。

原因在于中断服务程序中不能执行耗时操作，因此将相对耗时的操作交给小队列处理，再统一汇入大队列。大队列用于容纳不同设备的消息，小队列则用于容纳单个设备的消息。



* 2026-08-11 - 1