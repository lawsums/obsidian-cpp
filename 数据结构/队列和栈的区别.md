

| 维度   | 栈                                                                                                                                                                | 队列                                                                                                                       |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 进出顺序 | 先进后出                                                                                                                                                             | 先进先出                                                                                                                     |
| 操作端  | 单端操作                                                                                                                                                             | 两端操作                                                                                                                     |
| 应用场景 | 适合需要 “回溯、暂存最近数据” 的场景，如：<br><br>- 程序的 “函数调用栈”（函数嵌套调用时，先执行完内层函数再返回外层）；<br>- 括号匹配校验（如代码中`{()[]}`的合法性判断，遇到右括号时匹配最近的左括号）；<br>- 浏览器的 “后退功能”（记录最近访问的页面，后退时先返回上一个打开的页面）。 | 适合需要 “按顺序处理” 的场景，如：<br><br>- 操作系统的 “进程调度”（按申请顺序分配 CPU 资源）；<br>- 消息队列（如聊天软件的消息发送，按发送顺序依次传递）；<br>- 打印机的 “打印队列”（按提交顺序依次打印）。 |
| 数组实现 | 内存只与当前有<br>多少元素相关                                                                                                                                                | 每一个加入的元素都<br>会占据一份新的内存                                                                                                   |
